# Finding status matrix

Allowed terminal states are used exactly: `FIXED`, `NOT APPLICABLE`,
`RESIDUAL PLATFORM LIMITATION`, `BLOCKED BY PRODUCTION ACCESS`, and
`REQUIRES EXPLICIT PRODUCT DECISION`.

| ID | Corrected severity | Re-verification and root cause | Fix | Evidence | Commit | Status | Residual risk |
|---|---|---|---|---|---|---|---|
| LIO-H01 | High | Confirmed: a recovery-derived value authenticated device requests, so recovery could reproduce a device credential. | Device-auth v2 uses a random per-device secret wrapped by a non-extractable IndexedDB key; manifests and requests bind account, device and session. Recovery creates a distinct device and security event. | `securityFoundations.test.js`, `cryptoV4.integration.test.js`, browser MLS suite | `9cf881f` | FIXED | XSS in the active browser origin can still act as that browser device; see M03. |
| LIO-H02 | High | Confirmed: per-request limits allowed aggregate encrypted-media resource abuse. | Authorization before multipart; signed byte reservation; durable multi-scope upload/download byte, request, concurrency, storage and object quotas; bounded ranges. | `securityFoundations.test.js`, integration media cases, `mediaStorageRegression.js` | `d1d2796`, `764aa6e` | FIXED | Production thresholds require observation and tuning, not removal of the invariant. |
| LIO-H03 | High | Confirmed: concurrent avatar writes could orphan a public R2 object and had no pre-body limiter. | Pre-body guard and limiter, upload lease, object ownership record, CAS replacement, loser cleanup, retry/dead-letter state and dry-run reconciliation. | avatar race/orphan integration cases; security regression | `00f3a0d` | FIXED | Existing production orphans must be counted with the read-only checklist. |
| LIO-M01 | Medium | Confirmed: Socket.IO authenticated only at handshake and could outlive JWT expiry. | Sanitized JWT claims preserve `iat`/`exp`; exact expiry timer plus live session/device checks disconnect stale sockets. | socket expiry/revoke integration cases | `d1d2796` | FIXED | Network partitions may delay delivery, but the server authorization window does not extend past expiry. |
| LIO-M02 | Medium | Confirmed, but the old report overstated what server signatures alone proved. | Append-only RFC6962-style Merkle log, Ed25519 checkpoints, inclusion/consistency proofs, local pin continuity and checkpoint gossip. | transparency rollback/fork/proof unit and integration cases | `14a2567` | RESIDUAL PLATFORM LIMITATION | No independent witness or globally consistent gossip service exists; a sufficiently isolated client population can still receive a coordinated split view. |
| LIO-M03 | Medium | Confirmed: browser storage has no portable hardware-backed/XSS isolation. | Device secret is no longer recovery-derived, is encrypted under a non-extractable WebCrypto key in IndexedDB, and is separated from MLS/recovery records. CSP is tightened. | device storage/migration/downgrade tests; CSP static gate | `9cf881f`, `d1d2796` | RESIDUAL PLATFORM LIMITATION | Same-origin script compromise can invoke keys even when it cannot export them. Native platform keystores require a future native client boundary. |
| LIO-M04 | Medium | Confirmed: broad proxy trust allowed spoofed forwarded identity. | Exact trusted CIDR topology, proxy-protocol validation, host/origin guards and spoofing regression tests. | proxy/XFF unit and integration cases; VPS audit | `d1d2796` | FIXED | Production Nginx/Cloudflare topology must match the configured CIDRs; verify read-only after deployment. |
| LIO-M05 | Medium | Confirmed: legacy server-readable messages and metadata remain possible in production data. | Legacy routes are `410`; no new legacy writes or plaintext fallback; resumable count-only dry run and object-first retirement tooling with retry/dead-letter support. | legacy no-write/dry-run tests; security/privacy audits | `764aa6e` | REQUIRES EXPLICIT PRODUCT DECISION | Applying retirement irreversibly deletes real legacy data. The owner must choose retention vs deletion after reviewing aggregate production counts. |
| LIO-M06 | Medium | Confirmed: required secrets were not checked for independence. | Startup validation requires distinct JWT, email, recovery, manifest and transparency secrets and separates media/avatar R2 credentials and buckets. | `startupSecurityValidation.test.js`, security regression | `d1d2796` | FIXED | Actual production values must be checked without printing them. |
| LIO-M07 | Medium | Confirmed as a repository-platform gap: a historical CodeQL check was green while alerts remained open. Several concrete sinks were also real. | Concrete path, property, query and TOCTOU sinks fixed; workflow/security static gates added; CodeQL workflow pinned and narrowed. | input-guard tests, workflow audit, security regression | `ae0d9b3`, `4036aa9`, `7d63c88` | BLOCKED BY PRODUCTION ACCESS | A branch CodeQL scan and GitHub ruleset requiring acceptable code-scanning results must be verified/configured by the owner. No alert was silently dismissed. |
| LIO-L01 | Low | Confirmed: OIDC/attestation write capabilities were granted too broadly. | Default workflow permissions are read-only; attestation is isolated in a scoped job; third-party actions are full-SHA pinned. | `workflowSecurityAudit.js`, installer/release tests | `2bccf27` | FIXED | GitHub environment reviewers and branch rules remain platform settings. |
| LIO-L02 | Low | Confirmed: “delete for everyone” was an event, not cryptographic erasure. | Mutation v2 binds sender, target identity, revision and previous mutation; replay/stale/fork controls fail closed; delete is terminal; local cached plaintext/media are purged; server sequences media deletion. | `messageMutations.test.js`, integration convergence/race cases | `5b69187` | RESIDUAL PLATFORM LIMITATION | A recipient can retain screenshots, plaintext, ciphertext plus old keys, or an offline copy. The UI must not promise remote cryptographic erasure. |
| LIO-L03 | Low | Confirmed as misleading dead policy code, not the active media implementation. | Unreachable voice/call “secure” modules were proven unreachable and removed; active MLS media documentation now describes the actual AES-GCM chunk format. | import graph; crypto static analysis; browser MLS suite | `4036aa9` | FIXED | Calls remain unavailable; no voice-security claim is made. |
| LIO-L04 | Low | Confirmed: root tooling graph contained two high advisories in `brace-expansion` and `fast-uri`. | Lockfile refreshed to patched transitive versions. | root/client/server `npm audit`; clean install | `2569d43` | FIXED | Future advisories remain a continuous supply-chain concern. |
| LIO-L05 | Low | Confirmed: policy promised private reporting without an actual external channel. | Policy now names maintainer draft advisories and explicitly states that external Private Vulnerability Reporting is not enabled. | security-policy regression | `7d63c88` | REQUIRES EXPLICIT PRODUCT DECISION | Owner must enable GitHub PVR or publish/operate another private channel before inviting external reports. |
| LIO-I01 | Informational | The 72-hour value is an MLS self-update policy, not a reusable transport “session key” lifetime; membership commits also rotate epochs. No exploit was demonstrated. | No unsafe claim retained; expiry/epoch invariants remain tested. | crypto static and browser MLS tests | `714627a` | NOT APPLICABLE | The chosen self-update interval is still a product/security tradeoff. |
| LIO-I02 | Informational | Confirmed audit boundary: exact MLS primitive internals live in versioned CoreCrypto/WASM dependencies. | Dependency pinning, build integrity and black-box/invariant tests cover the repository boundary. | lock consistency, SBOM, crypto invariants | `2bccf27` | RESIDUAL PLATFORM LIMITATION | A dedicated upstream/CoreCrypto source audit is separate work. |
| LIO-I03 | Informational | Confirmed platform boundary: browser APIs do not provide a portable hardware-backed key contract. | Non-extractable WebCrypto wrapping and credential separation are the strongest portable browser hardening used. | device storage tests | `9cf881f` | RESIDUAL PLATFORM LIMITATION | Hardware-backed guarantees require native wrappers and platform attestation. |
| LIO-I04 | Informational | Confirmed: the previous narrow module percentage was presented too broadly. | Coverage is now described by suite type; security uses explicit invariants and architecture gates rather than a misleading project-wide percentage. | unit, integration, browser, migration, static and release suites | `714627a`, `4036aa9` | FIXED | Untested product behavior can still exist; the evidence never claims total line coverage. |
| LIO-I05 | Informational | Confirmed: calls were disabled while misleading legacy/call modules remained. | Proven unreachable call/voice and legacy production modules were removed; current capability remains explicitly unavailable. | complete import graph and deletion locks | `4036aa9` | FIXED | Calling functionality would require a new reviewed protocol, not reactivation of deleted code. |
| LIO-N01 | High | New re-audit finding: attacker-derived file paths, dynamic properties/Mongo keys and release path re-open races existed. | Managed upload closures remove path injection; explicit allowlists replace dynamic writes/queries; release verification holds and checks the opened descriptor. | `securityInputGuards.test.js`, release/security regressions | `ae0d9b3` | FIXED | CodeQL branch results still require platform verification under M07. |
| LIO-N02 | High | New re-audit finding: encrypted edit/delete controls lacked a deterministic target/revision chain across devices. | Message mutation v2 chain, terminal deletes, atomic encrypted local materialization, cutoff migration and server media sequence checks. | `messageMutations.test.js`, integration and browser tests | `5b69187` | FIXED | Remote erasure limitation remains L02. |
| LIO-N03 | High | New re-audit finding: artifacts could be reproducible without proving they came from the reviewed commit. | Source SHA in archive/deployment/client manifests, clean-tree assertion, installer cross-check, GitHub attestation verification and reproducible bundles. | release, archive, deployment and installer regressions | `2bccf27` | FIXED | Trust still depends on GitHub identity/attestation and controlled deployment credentials. |
| LIO-N04 | Medium | New re-audit finding: unreferenced legacy/security modules could drift into misleading duplicate implementations. | Full relative import graph from actual runtime/operational roots; 17 transitively unreachable modules removed; deletion regression locks. | `codeHealthAudit.js`, security regression | `4036aa9` | FIXED | Dynamic imports outside the supported patterns must be added to the audit before use. |
